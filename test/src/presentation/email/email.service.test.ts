import nodemailer from "nodemailer";
import {EmailService, SendMailOptions} from "../../../../src/presentation/email/email.service";

describe("EmailService",()=>{

    // const mockSendMailer = jest.fn();
    // nodemailer.createTransport(jest.fn().mockReturnValue({
    //     sendMail: mockSendMailer
    // }));

    test("Should send email", async ()=>{
    //     const emailService = new EmailService();
    //
    //     const options: SendMailOptions = {
    //         htmlBody: "<h1>Test</h1>", subject: "Test", to: 'topomajor1@hotmail.comassda'
    //     }
    //
    //
    //
    //     await emailService.sendEmail(options);
    //
    //     expect(mockSendMailer).toHaveBeenCalledWith({});
    });
});